# CS469 HW1 - A* Navigation for a Differential Drive Robot

Author: Conor Hayes

## Setup Instructions
### Using `uv` (recommended)
`uv sync`

### Using `pip`
`pip install -r requirements.txt`

## Run Instructions
`uv run run.py` or `python3 run.py`

This will generate all plots & figures from the writeup